import { execute } from ".";
import { getProfileData } from ".";

export async function updateFavorite(name_id: string, newState: boolean) {
  // const [profileData] = await getProfileData();
  // console.log(profileData);
}
